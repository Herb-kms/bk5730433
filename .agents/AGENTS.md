# Project Specific Rules

- **Git Push Policy**: Do NOT automatically perform `git push` after code edits or commits. Only execute `git push` when the user explicitly requests to upload/push to GitHub ("깃허브에 올려줘", "git push 해줘" 등).
